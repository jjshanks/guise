# Build output
/urlrouter.exe

# The resource .syso IS committed so `go build` is turnkey (it carries the
# manifest walk needs at runtime); regenerate it with `go generate`.

# Claude Code worktrees / session state
.claude/
